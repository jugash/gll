{{- if .Values.redis.enabled }}
{{- /*
Single-primary Redis/Valkey (engine selectable via redis.engine) with password
auth and persistence. It writes only to its data dir, so it runs cleanly under
`restricted-v2` with an arbitrary UID + fsGroup (no uid resolution needed).

For Sentinel-based HA in-cluster, do NOT hand-roll a fragile Sentinel topology
here — deploy HA Redis/Valkey via a Redis/Valkey operator (see installation-guide.md)
and set redis.sentinel.enabled=true so the GitLab CR is wired for Sentinel.
Redis and Valkey are wire-compatible, so GitLab connects to either unchanged.
*/ -}}
apiVersion: v1
kind: Service
metadata:
  name: gitlab-redis
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "deps.labels" . | nindent 4 }}
spec:
  selector: { app: gitlab-redis }
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gitlab-redis
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "deps.labels" . | nindent 4 }}
spec:
  serviceName: gitlab-redis
  replicas: 1
  selector:
    matchLabels: { app: gitlab-redis }
  template:
    metadata:
      labels:
        app: gitlab-redis
        {{- include "deps.labels" . | nindent 8 }}
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: redis
          image: {{ include "deps.redisImage" . }}
          command:
            - {{ include "deps.redisServerBin" . }}
            - --requirepass
            - $(REDIS_PASSWORD)
            - --appendonly
            - "yes"
            - --dir
            - /data
          ports:
            - containerPort: 6379
              name: redis
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef: { name: gitlab-redis, key: password }
          readinessProbe:
            exec:
              command: ["/bin/sh","-c","{{ include "deps.redisCliBin" . }} -a \"$REDIS_PASSWORD\" ping | grep -q PONG"]
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket: { port: 6379 }
            initialDelaySeconds: 20
            periodSeconds: 15
          resources:
            {{- toYaml .Values.redis.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- with .Values.global.storageClass }}
        storageClassName: {{ . }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.redis.persistence.size }}
{{- end }}
